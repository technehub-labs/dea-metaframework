name: CI
on:
  pull_request:
    branches: [main]
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install pyyaml jsonschema pytest
      - run: python scripts/regenerate_catalog.py --check
      - run: python scripts/check_catalog_index.py
